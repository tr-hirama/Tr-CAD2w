{
  "name": "tr-cad2w",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Speed-first 2D survey CAD for the browser (TypeScript + Canvas)",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "vite": "^7.1.3",
    "vitest": "^3.2.4"
  }
}
